# Secrets — never commit
.env

# Virtual environment
venv/

# Python bytecode
__pycache__/
*.pyc

# Raw eval outputs (EVAL_LOG.md is committed; per-run JSON is not)
evals/results/*.json

# Local-only working docs — not part of the deliverable
PROMPTS.md
CLAUDE.local.md
PLAN.local.md
